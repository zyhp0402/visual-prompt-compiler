# AGENTS.md

## 1. 项目使命

构建一个 Chrome Manifest V3 侧边栏扩展，将用户的模糊视觉需求编译成可直接用于 GPT Image 2 的高质量提示词。

本项目的成功标准不是“生成更长的提示词”，而是：

1. 更准确地保留用户硬约束；
2. 更清楚地表达主体、空间关系和视觉层级；
3. 更少地产生互相矛盾的构图、光线、材质与文字要求；
4. 更稳定地产生三个真正有差异的视觉方向；
5. 能通过固定数据集、自动规则和人工偏好测试持续验证改进。

## 2. 单一事实来源

- 产品与技术范围：`PLAN.md`
- 当前里程碑指令：`TASK_PROMPTS.md`
- 数据契约：`schemas/`
- 评测输入：`fixtures/`
- 实施决策：`docs/adr/`
- 任务状态：`docs/status.md`

发现冲突时，优先级为：

1. 用户在当前 Codex 任务中的明确指令；
2. `PLAN.md`；
3. `AGENTS.md`；
4. 其他文档；
5. 现有实现。

不要静默解决重大冲突。将其记录到 `docs/open-questions.md`。

## 3. 范围纪律

### MVP 必须包含

- Chrome Side Panel；
- 简报输入与约束表单；
- `VisualSpec` 中间结构；
- 三种方向：稳妥、创意、实验；
- 确定性提示词检查器；
- 服务端 OpenAI API 调用；
- 结构化 JSON 返回；
- 历史记录和收藏，保存在 `chrome.storage.local`；
- 基准数据集与可重复评测；
- 单元、集成和端到端测试。

### MVP 明确不包含

- 用户注册和多租户；
- 支付；
- 社区和公开案例广场；
- 模型微调；
- 自动抓取整个 GitHub；
- 自动操纵 ChatGPT 网页 DOM；
- 大规模向量数据库；
- 团队协作；
- 批量图片生产；
- 浏览器中保存 OpenAI API Key；
- 对“美观”作绝对保证。

除非 `PLAN.md` 被用户明确更新，不得加入这些功能。

## 4. 工程工作方式

1. 一次只实施一个里程碑。
2. 修改前先阅读相关代码、Schema、测试和 ADR。
3. 先写或更新测试，再完成实现。
4. 每个里程碑结束时必须运行全部相关检查。
5. 不得用注释、空函数、假数据或永久 mock 冒充完成。
6. 外部 API 在测试中必须被隔离；端到端测试不得消耗真实 OpenAI 额度。
7. 代码与文档必须同步更新。
8. 任何会改变数据契约、API 或核心架构的决策，都新增 ADR。
9. 不得把密钥、真实用户提示词、参考图片或个人数据写入日志。
10. 不得绕过 TypeScript 类型检查、ESLint 或测试失败。

## 5. 推荐技术约束

除非任务 0 的架构审查提出充分理由并写 ADR，否则使用：

- Monorepo：pnpm workspaces；
- 语言：TypeScript，开启 strict；
- 扩展：React + Vite + Chrome Manifest V3；
- API：Node.js + Fastify；
- 数据校验：Zod；
- 测试：Vitest + Playwright；
- OpenAI：官方 JavaScript/TypeScript SDK；
- API 形态：Responses API；
- 文本模型与图像模型必须通过环境变量配置；
- 默认图像模型：`gpt-image-2`；
- 历史与收藏：`chrome.storage.local`；
- MVP 不依赖数据库。

## 6. 代码质量

- 函数保持单一职责；
- 业务逻辑不得直接耦合 React 组件或 Fastify handler；
- 核心编译器必须是可独立测试的纯模块；
- 所有外部输入必须在边界层验证；
- 不使用 `any`，除非局部且有解释；
- 错误需使用稳定错误码，不把内部堆栈暴露给扩展；
- 所有用户可见字符串集中管理，预留国际化；
- 生成提示词不得依赖不可追踪的随机拼接；
- 任何模型调用必须记录匿名化的模型名、提示词版本、耗时、token/成本元数据和结果状态，但不得记录敏感正文。

## 7. UI 规则

- 侧边栏首先服务于桌面 Chrome；
- 默认中文界面；
- 主流程在一个页面完成；
- 高级参数折叠；
- 每个方向必须显示：概念说明、完整提示词、关键假设、风险、复制按钮；
- 未解决的硬约束必须在结果上方显著提示；
- 加载、失败、超时、重试、无网络状态必须完整实现；
- 不使用只有图标而无可访问名称的按钮；
- 键盘可操作；
- 颜色对比、焦点状态和表单标签必须可访问。

## 8. 安全与隐私

- OpenAI API Key 只存在服务端环境变量；
- 服务端限制请求体大小；
- 参考图上传功能进入后续阶段，加入时必须验证 MIME、大小和像素；
- CORS 仅允许配置的扩展来源；
- 加入速率限制和超时；
- 默认不持久化用户原始简报；
- 错误日志需脱敏；
- 案例数据必须包含来源与许可证字段；
- 无法确认权利状态的案例不得进入可发布数据包。

## 9. 完成定义

任务只有在以下条件全部满足时才算完成：

- 实现符合当前里程碑；
- 测试全部通过；
- TypeScript、lint、format 检查通过；
- README 或相关文档已更新；
- 无未解释的 TODO/FIXME；
- 验收标准有证据；
- `docs/status.md` 已更新；
- 变更范围没有越过当前里程碑；
- Codex 给出已运行命令和结果摘要。
