{
  "name": "visual-prompt-compiler",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @vpc/api --filter @vpc/extension dev",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "test:e2e": "playwright test",
    "eval:mock": "pnpm build && node packages/evals/dist/cli.js --mode mock",
    "eval:cases:mock": "pnpm build && node packages/evals/dist/case-cli.js",
    "eval:real": "node scripts/eval-real-preflight.mjs && pnpm build && node packages/evals/dist/cli.js --mode real",
    "eval:versions": "pnpm --filter @vpc/contracts build && pnpm --filter @vpc/compiler-core build && pnpm --filter @vpc/openai-adapter build && pnpm --filter @vpc/evals build && node packages/evals/dist/version-cli.js check",
    "eval:approve": "pnpm --filter @vpc/contracts build && pnpm --filter @vpc/compiler-core build && pnpm --filter @vpc/openai-adapter build && pnpm --filter @vpc/evals build && node packages/evals/dist/version-cli.js approve",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json && pnpm -r --if-present typecheck",
    "format:check": "prettier --check .",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build && node packages/evals/dist/version-cli.js check && pnpm test:e2e"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.61.1",
    "@types/node": "26.1.1",
    "eslint": "10.8.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10"
  }
}
